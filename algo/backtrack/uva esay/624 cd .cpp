#include<iostream>
using namespace std;

int main()
{
    int n,tno,arr[26];
    while(cin>>n)
    {

        cin>>tno;
    int flug[tno+1];
        for(int i=1; i<=tno; i++)
        {
            cin>>arr[i];
        }
        for(i=1;i<=tno;i++)
        {
            for(int j=1;j<=tno;j++)
            {
                if(arr[i])
            }
        }
    }
}
